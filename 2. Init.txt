GitHub Commands

        1. Initialize project in  local repo 
                git init myproject

        2. In case project already exist (to add .git folder)
                git init

        3. Check the status
                git status